program Ex047;
var a, b : longint;

{ programa principal }
begin

    read(a, b);

    if (not(odd(a)) or not(odd(b))) or (not(a <= b)) then
    begin
        write('erro');
    end
    else
    begin
        
    end;

    writeln;

end.

